name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  validate-plugin:
    name: Validate plugin structure
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - name: Check required plugin files exist
        run: |
          errors=0

          # Plugin manifest
          for f in .claude-plugin/plugin.json .claude-plugin/marketplace.json; do
            if [ ! -f "$f" ]; then
              echo "::error::Missing required file: $f"
              errors=$((errors + 1))
            else
              echo "OK: $f"
            fi
          done

          if [ "$errors" -gt 0 ]; then
            echo "::error::$errors required file(s) missing"
            exit 1
          fi
          echo "All required files present."

      - name: Validate plugin.json is valid JSON
        run: |
          for f in .claude-plugin/plugin.json .claude-plugin/marketplace.json; do
            if ! python3 -m json.tool "$f" > /dev/null 2>&1; then
              echo "::error::Invalid JSON in $f"
              exit 1
            fi
            echo "OK: $f is valid JSON"
          done

      - name: Validate markdown frontmatter
        run: |
          errors=0

          # Agents must have name and description
          for f in agents/*.md; do
            [ ! -f "$f" ] && continue
            if ! head -1 "$f" | grep -q '^---$'; then
              echo "::error file=$f::Missing YAML frontmatter (no opening ---)"
              errors=$((errors + 1))
              continue
            fi
            # Extract frontmatter (between first and second ---)
            frontmatter=$(sed -n '2,/^---$/p' "$f" | sed '$d')
            if ! echo "$frontmatter" | grep -q '^name:'; then
              echo "::error file=$f::Agent missing 'name' in frontmatter"
              errors=$((errors + 1))
            fi
            if ! echo "$frontmatter" | grep -q '^description:'; then
              echo "::error file=$f::Agent missing 'description' in frontmatter"
              errors=$((errors + 1))
            fi
            echo "OK: $f"
          done

          # Commands must have description
          for f in commands/*.md; do
            [ ! -f "$f" ] && continue
            if ! head -1 "$f" | grep -q '^---$'; then
              echo "::error file=$f::Missing YAML frontmatter (no opening ---)"
              errors=$((errors + 1))
              continue
            fi
            frontmatter=$(sed -n '2,/^---$/p' "$f" | sed '$d')
            if ! echo "$frontmatter" | grep -q '^description:'; then
              echo "::error file=$f::Command missing 'description' in frontmatter"
              errors=$((errors + 1))
            fi
            echo "OK: $f"
          done

          # Skills must have description
          for f in skills/*.md skills/*/SKILL.md; do
            [ ! -f "$f" ] && continue
            if ! head -1 "$f" | grep -q '^---$'; then
              echo "::error file=$f::Missing YAML frontmatter (no opening ---)"
              errors=$((errors + 1))
              continue
            fi
            frontmatter=$(sed -n '2,/^---$/p' "$f" | sed '$d')
            if ! echo "$frontmatter" | grep -q '^description:'; then
              echo "::error file=$f::Skill missing 'description' in frontmatter"
              errors=$((errors + 1))
            fi
            echo "OK: $f"
          done

          if [ "$errors" -gt 0 ]; then
            echo "::error::$errors frontmatter issue(s) found"
            exit 1
          fi
          echo "All markdown frontmatter valid."

      - name: Check no legacy terms are present
        run: |
          legacy_ext="r""ac"
          source_initials="A""K""N"
          source_name="Ako""ma"
          if find . -path './.git' -prune -o -type f -print \
            | xargs grep -InE "\\b${legacy_ext}\\b|\\b${legacy_ext^^}\\b|\\.${legacy_ext}\\b|auto${legacy_ext}|${source_name}|\\b${source_initials}\\b|\\.${source_initials,,}\\b"; then
            echo "::error::Legacy syntax or source-format term found"
            exit 1
          fi
          echo "No legacy terms found."
